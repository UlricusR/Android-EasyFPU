<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.rueth.fpucalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NewFoodActivity"
            android:label="@string/title_activity_newfood">
        </activity>
        <activity
            android:name=".EditFoodActivity"
            android:label="@string/title_activity_editfood">
        </activity>
        <activity
            android:name=".NewMealActivity"
            android:label="@string/title_activity_newmeal">
        </activity>
        <activity
            android:name=".CalcMealActivity"
            android:label="@string/title_activity_calcmeal">
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PreferencesActivity"
            android:label="@string/title_activity_preferences"
            android:parentActivityName=".MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="info.rueth.fpucalculator.MainActivity" />
        </activity>
    </application>

</manifest>